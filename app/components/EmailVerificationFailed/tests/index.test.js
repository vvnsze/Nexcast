// import React from 'react';
// import { shallow } from 'enzyme';

// import EmailVerificationFailed from '../index';

describe('<EmailVerificationFailed />', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
