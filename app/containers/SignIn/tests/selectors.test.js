// import { fromJS } from 'immutable';
// import { makeSelectSignInDomain } from '../selectors';

// const selector = makeSelectSignInDomain();

describe('makeSelectSignInDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
