// import { fromJS } from 'immutable';
// import { makeSelectSearchPodcastDomain } from '../selectors';

// const selector = makeSelectSearchPodcastDomain();

describe('makeSelectSearchPodcastDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
