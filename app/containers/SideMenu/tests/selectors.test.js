// import { fromJS } from 'immutable';
// import { makeSelectSideMenuDomain } from '../selectors';

// const selector = makeSelectSideMenuDomain();

describe('makeSelectSideMenuDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
